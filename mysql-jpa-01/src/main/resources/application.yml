log:
    level:
        root: INFO
        org: INFO
        com: INFO
        org.springframework: INFO
        org.hibernate: INFO
        org.hibernate.SQL: DEBUG
        org.hibernate.type: TRACE
spring:
    application:
        name: mysql-jpa-01
    datasource:
        url: jdbc:mysql://localhost:3306/library_db?useSSL=false&useUnicode=true&characterEncoding=UTF-8
        username: devUser
        password: 5382
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: create
        show-sql: true
    session:
        store-type: redis
        redis:
            namespace: myapp:sessions